// Declare an array that stores a shopping list.
// This array should have three categories (arrays) of the
// shopping items and each of these categories should be
// an array with at least three shopping items.
// Log the 3rd item of the 2nd list of items to the console.

let shopping = [["potato", "tomato", "leek"] , ["coke", "wine", "juice"], ["flour", "sugar"] ];
console.log(shopping[1][2]);

